apiVersion: v1
kind: Secret

metadata:
  name: postgres-secret
  namespace: storage

  annotations:
    governance.core/dev-only: "true"

type: Opaque

stringData:
  username: postgres
  password: corei-postgres-dev-password
