apiVersion: v1
kind: ConfigMap

metadata:
  name: market-data-config
  namespace: market-data

data:
  KAFKA_BOOTSTRAP_SERVERS: "kafka.kafka.svc.cluster.local:9092"

  RAW_MARKET_TOPIC: "raw_market_data_stream"

  ENVIRONMENT: "development"
