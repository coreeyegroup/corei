apiVersion: rbac.authorization.k8s.io/v1
kind: Role

metadata:
  name: market-data-role
  namespace: market-data

rules:
  - apiGroups: [""]
    resources:
      - pods
      - services
      - configmaps

    verbs:
      - get
      - list
      - watch
