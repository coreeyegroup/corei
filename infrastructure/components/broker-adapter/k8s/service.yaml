apiVersion: v1
kind: Service

metadata:
  name: broker-adapter-service
  namespace: trading-infra

spec:
  selector:
    app: broker-adapter-service

  ports:
    - protocol: TCP
      port: 8003
      targetPort: 8003
