--- scripts/security/kafka/v2/python/manifest_generator.py	2026-05-30 21:33:57.119455126 +0000
+++ scripts/security/kafka/v2/python/manifest_generator.py.stage21w05.bak	2026-05-30 16:06:38.357068737 +0000
@@ -16,38 +16,13 @@
     "state/security/kafka/v2/manifests/secured-statefulset.yaml"
 )

-PROFILE = "RECOVERY"
-

 def ensure_list(obj, key):
-
     if key not in obj or obj[key] is None:
         obj[key] = []
-
     return obj[key]


-def build_security_block():
-
-    if PROFILE == "RECOVERY":
-
-        return """
-sasl.enabled.mechanisms=SCRAM-SHA-256
-
-listener.name.sasl_ssl.sasl.enabled.mechanisms=SCRAM-SHA-256
-
-listener.name.sasl_ssl.scram-sha-256.sasl.jaas.config=org.apache.kafka.common.security.scram.ScramLoginModule required;
-
-ssl.keystore.location=/etc/kafka/tls/keystore.jks
-
-ssl.truststore.location=/etc/kafka/tls/truststore.jks
-"""
-
-    raise RuntimeError(
-        f"unsupported profile: {PROFILE}"
-    )
-
-
 def inject_runtime_security(command_text):

     command_text = command_text.replace(
@@ -65,13 +40,26 @@
         "listener.security.protocol.map=PLAINTEXT:PLAINTEXT,SASL_SSL:SASL_SSL,CONTROLLER:PLAINTEXT"
     )

+    security_block = """
+sasl.enabled.mechanisms=SCRAM-SHA-256
+
+listener.name.sasl_ssl.scram-sha-256.sasl.jaas.config=org.apache.kafka.common.security.scram.ScramLoginModule required;
+
+listener.name.sasl_ssl.sasl.enabled.mechanisms=SCRAM-SHA-256
+
+sasl.mechanism.inter.broker.protocol=SCRAM-SHA-256
+
+ssl.keystore.location=/etc/kafka/tls/keystore.jks
+
+ssl.truststore.location=/etc/kafka/tls/truststore.jks
+"""
+
     marker = "transaction.state.log.min.isr=1"

     if marker in command_text:
-
         command_text = command_text.replace(
             marker,
-            marker + "\n" + build_security_block().strip()
+            marker + "\n" + security_block.strip()
         )

     return command_text
@@ -96,14 +84,11 @@
     kafka_container = None

     for container in pod_spec["containers"]:
-
         if container["name"] == "kafka":
-
             kafka_container = container
             break

     if kafka_container is None:
-
         raise RuntimeError(
             "kafka container not found"
         )
@@ -120,6 +105,13 @@
         }
     })

+    volumes.append({
+        "name": plan["jaas_volume"],
+        "secret": {
+            "secretName": plan["jaas_volume"]
+        }
+    })
+
     mounts = ensure_list(
         kafka_container,
         "volumeMounts"
@@ -131,6 +123,23 @@
         "readOnly": True
     })

+    mounts.append({
+        "name": plan["jaas_volume"],
+        "mountPath": plan["jaas_mount"],
+        "readOnly": True
+    })
+
+    env = ensure_list(
+        kafka_container,
+        "env"
+    )
+
+    env.append({
+        "name": plan["env_var"],
+        "value":
+        "-Djava.security.auth.login.config=/etc/kafka/security/kafka_jaas.conf"
+    })
+
     ports = ensure_list(
         kafka_container,
         "ports"
@@ -142,13 +151,9 @@
         "protocol": "TCP"
     })

-    args = kafka_container.get(
-        "args",
-        []
-    )
+    args = kafka_container.get("args", [])

     if args:
-
         args[0] = inject_runtime_security(
             args[0]
         )
@@ -157,7 +162,6 @@
         OUTPUT_FILE,
         "w"
     ) as f:
-
         yaml.safe_dump(
             manifest,
             f,
