apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cicd
  namespace: cicd

spec:
  project: default

  source:
    repoURL: https://github.com/corei-org/core-platform-gitops.git
    targetRevision: main
    path: gitops/base/platform/cicd

  destination:
    server: https://kubernetes.default.svc
    namespace: cicd

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
