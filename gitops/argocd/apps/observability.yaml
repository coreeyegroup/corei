apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: observability
  namespace: cicd

spec:
  project: default

  source:
    repoURL: https://github.com/coreeyegroup/corei.git
    targetRevision: main
    path: gitops/base/platform/observability

  destination:
    server: https://kubernetes.default.svc
    namespace: observability

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
