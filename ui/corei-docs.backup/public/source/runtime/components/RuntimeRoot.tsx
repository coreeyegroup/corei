import {

    RuntimeProvider

} from "../providers/runtime-provider";

export function RuntimeRoot(): React.JSX.Element {

    return (

        <RuntimeProvider>

            RUNTIME ROOT

        </RuntimeProvider>

    );

}
