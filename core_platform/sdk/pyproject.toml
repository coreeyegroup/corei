[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "trading-platform-sdk"
version = "0.1.0"
description = "Institutional Trading Platform SDK"
authors = [
    {name = "CORE SYSTEM"}
]
dependencies = [
    "httpx==0.27.0",
    "pydantic==2.8.2",
    "aiokafka==0.10.0",
    "confluent-kafka==2.5.3"
]
