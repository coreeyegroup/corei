# PERFORMANCE GOVERNANCE

All pipeline stages MUST:

- satisfy latency budgets
- generate deterministic benchmark artifacts
- preserve replay-safe execution timing

Latency violations are CI-blocking failures.
