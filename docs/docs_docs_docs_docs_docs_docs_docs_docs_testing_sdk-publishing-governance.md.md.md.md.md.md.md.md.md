# SDK PUBLISHING GOVERNANCE

All platform services MUST install the SDK from Harbor PyPI registry.

Forbidden:
- local SDK copies
- editable installs in production
- unversioned SDK installs
- direct filesystem SDK coupling

SDK versions are immutable once published.
