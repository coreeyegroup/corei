# ORCHESTRATED TESTING GOVERNANCE

All institutional testing execution MUST:

- follow dependency-aware ordering
- preserve deterministic execution
- generate centralized artifacts
- preserve replay-safe execution

Execution graph violations are CI-blocking failures.
