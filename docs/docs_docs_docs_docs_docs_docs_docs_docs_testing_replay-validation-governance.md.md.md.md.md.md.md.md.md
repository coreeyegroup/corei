# REPLAY VALIDATION GOVERNANCE

Replay validation requires:

- deterministic ordering
- canonical serialization
- identical hashes
- identical outputs
- lineage traceability

Replay mismatch is a CI-blocking failure.
