apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: operational-controller-role
  namespace: platform
rules:
  - apiGroups: [""]
    resources:
      - pods
      - services
      - configmaps
    verbs:
      - get
      - list
      - watch

  - apiGroups: ["apps"]
    resources:
      - deployments
    verbs:
      - get
      - list
      - watch
