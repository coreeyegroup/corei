apiVersion: v1
kind: Service

metadata:
  name: python-execution-service
  namespace: platform

spec:
  selector:
    app: python-execution-service

  ports:
    - port: 8000
      targetPort: 8000

  type: ClusterIP
