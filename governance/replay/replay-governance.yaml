replay:
  deterministic: true
  replay_safe: true
  canonical_ordering_required: true
  identical_hash_required: true

validation:
  dual_run_validation: mandatory
  bit_level_equivalence: mandatory
  lineage_validation: mandatory

ci:
  replay_failure_blocks_pipeline: true
